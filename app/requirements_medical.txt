# Web App dependencies
# Install with: pip install -r app/requirements_medical.txt

# Web application
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Gemini LLM + embeddings (clinical simulation + case search)
google-genai>=1.0.0

# MultiCaRe dataset (real PubMed Central case reports)
multiversity

# Environment variable loading
python-dotenv

python-multipart
